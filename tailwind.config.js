export default {
  plugins: {
    tailwindcss: {}, // This tells PostCSS to use the Tailwind CSS plugin
    autoprefixer: {}, // Autoprefixer is highly recommended for cross-browser compatibility
  },
};
